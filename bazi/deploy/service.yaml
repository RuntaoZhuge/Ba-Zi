apiVersion: v1
kind: Service
metadata:
  name: bazi-web
  namespace: bazi-prod
  labels:
    app: bazi-web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: bazi-web
